/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package placeholder.render;

/**
 * An interface to signify that a class is used to store render information.
 * Data fields will be populated from a file when a TileRenderer that is using this
 * class is attach to a tile.  The field names must correspond exactly to the 
 * fields in the rendering data file.
 * 
 * This class must have a constructor with no parameters, and the data fields 
 * populated by the rendering data file should not be changed once populated.
 * 
 * @author russell
 */
public interface RenderInfo {
}
